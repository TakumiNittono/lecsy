# lecsy iOS設計書

## 📋 目次

1. [概要](#概要)
2. [プロジェクト構成](#プロジェクト構成)
3. [アーキテクチャ](#アーキテクチャ)
4. [主要コンポーネント](#主要コンポーネント)
5. [データモデル](#データモデル)
6. [画面設計](#画面設計)
7. [設定と権限](#設定と権限)
8. [Live Activities](#live-activities)
9. [バックグラウンド処理](#バックグラウンド処理)
10. [Supabase連携](#supabase連携)

---

## 概要

### アプリ情報

| 項目 | 値 |
|------|-----|
| アプリ名 | lecsy |
| Bundle ID | `com.takumiNittono.lecsy` |
| Widget Bundle ID | `com.takumiNittono.lecsy.LecsyWidget` |
| App Group | `group.com.takumiNittono.lecsy` |
| 最小iOS | 17.6 |
| 言語 | Swift 5.0 |
| UI | SwiftUI |

### 技術要件

| 要件 | 対応 |
|------|------|
| 録音 | AVAudioRecorder |
| 文字起こし | WhisperKit (ローカル) |
| Live Activities | ActivityKit |
| 認証 | Supabase Auth |
| ローカルDB | SQLite / CoreData |
| バックグラウンド | Background Audio Mode |

---

## プロジェクト構成

```
lecsy/
├── lecsyApp.swift                    # アプリエントリーポイント
├── ContentView.swift                 # メインTabView
├── LecsyWidgetAttributes.swift       # Live Activities属性定義
│
├── Models/                           # データモデル
│   ├── Lecture.swift                # 講義データモデル
│   ├── TranscriptionResult.swift    # 文字起こし結果
│   ├── TranscriptionLanguage.swift  # 文字起こし言語
│   ├── TranscriptionStatus.swift    # 文字起こし状態
│   └── User.swift                   # ユーザーモデル
│
├── Services/                         # ビジネスロジック
│   ├── RecordingService.swift       # 録音サービス
│   ├── TranscriptionService.swift   # 文字起こしサービス
│   ├── LectureStore.swift           # 講義データ管理
│   ├── AuthService.swift            # 認証サービス
│   ├── SyncService.swift            # Web同期サービス
│   └── LanguageManager.swift        # 言語管理
│
├── Views/                            # UIコンポーネント
│   ├── Home/
│   │   └── RecordView.swift         # 録音画面
│   ├── Library/
│   │   ├── LibraryView.swift        # 講義一覧
│   │   └── LectureDetailView.swift  # 講義詳細
│   ├── Settings/
│   │   └── SettingsView.swift       # 設定画面
│   ├── Auth/
│   │   └── LoginView.swift          # ログイン画面
│   └── Components/
│       ├── StopwatchView.swift      # ストップウォッチ
│       ├── ProgressView.swift       # 進捗表示
│       └── SaveToWebButton.swift    # Web保存ボタン
│
├── Utils/                            # ユーティリティ
│   ├── LocalizedString.swift        # 多言語文字列
│   ├── DateFormatter+.swift         # 日付フォーマット
│   └── FileManager+.swift           # ファイル操作
│
└── Resources/
    ├── Assets.xcassets              # アセット
    └── Info.plist                   # 設定

LecsyWidget/                          # ウィジェット拡張
├── LecsyWidget.swift                # 標準ウィジェット
├── LecsyWidgetBundle.swift          # ウィジェットバンドル
└── LecsyWidgetLiveActivity.swift    # Live Activities UI
```

---

## アーキテクチャ

### パターン: MVVM

```
┌─────────────────────────────────────────────────────────────┐
│                        View Layer                           │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         │
│  │ RecordView  │  │ LibraryView │  │SettingsView│          │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘         │
│         │                │                │                 │
│         │     @StateObject / @ObservedObject                │
│         │                │                │                 │
└─────────┼────────────────┼────────────────┼─────────────────┘
          │                │                │
┌─────────▼────────────────▼────────────────▼─────────────────┐
│                      Service Layer                          │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         │
│  │ Recording   │  │ Lecture     │  │ Transcription│        │
│  │ Service     │  │ Store       │  │ Service      │        │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘         │
│         │                │                │                 │
│         └────────────────┼────────────────┘                 │
│                          │                                  │
└──────────────────────────┼──────────────────────────────────┘
                           │
┌──────────────────────────▼──────────────────────────────────┐
│                      Data Layer                             │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         │
│  │   SQLite/   │  │  FileManager │ │  UserDefaults│        │
│  │  CoreData   │  │  (Audio)    │  │  (Settings) │        │
│  └─────────────┘  └─────────────┘  └─────────────┘         │
└─────────────────────────────────────────────────────────────┘
```

### 状態管理

| コンポーネント | パターン | 役割 |
|---------------|---------|------|
| RecordingService | Singleton + @MainActor | 録音状態・タイマー管理 |
| TranscriptionService | Singleton + @MainActor | 文字起こし状態・進捗管理 |
| LectureStore | @MainActor | 講義データの永続化 |
| AuthService | Singleton | 認証状態管理 |
| SyncService | Singleton | Web同期管理 |
| LanguageManager | Singleton | 言語設定管理 |

---

## 主要コンポーネント

### 1. RecordingService

**役割**: 音声録音の全てを管理

```swift
@MainActor
class RecordingService: ObservableObject {
    // MARK: - Published Properties
    @Published var isRecording: Bool = false
    @Published var recordingDuration: TimeInterval = 0
    @Published var audioLevel: Float = 0  // 音声レベルメーター用
    
    // MARK: - Private Properties
    private var audioRecorder: AVAudioRecorder?
    private var liveActivity: Activity<LecsyWidgetAttributes>?
    private var recordingStartTime: Date?
    private var timer: Timer?
    private var backgroundTask: UIBackgroundTaskIdentifier = .invalid
    
    // MARK: - Computed Properties
    var formattedStopwatch: String {
        // "00:00:00.00" 形式
    }
    
    // MARK: - Public Methods
    func startRecording() async throws
    func stopRecording() -> URL?
    func requestMicrophonePermission() async -> Bool
    
    // MARK: - Live Activities
    private func startLiveActivity()
    private func updateLiveActivity()
    private func endLiveActivity()
    
    // MARK: - Background
    private func setupBackgroundTask()
    private func endBackgroundTask()
}
```

**録音設定:**
```swift
let settings: [String: Any] = [
    AVFormatIDKey: Int(kAudioFormatMPEG4AAC),
    AVSampleRateKey: 44100,
    AVNumberOfChannelsKey: 1,
    AVEncoderAudioQualityKey: AVAudioQuality.high.rawValue
]
```

---

### 2. TranscriptionService

**役割**: WhisperKitによるローカル文字起こし

```swift
@MainActor
class TranscriptionService: ObservableObject {
    static let shared = TranscriptionService()
    
    // MARK: - Published Properties
    @Published var state: TranscriptionState = .idle
    @Published var progress: Double = 0
    @Published var transcriptionLanguage: TranscriptionLanguage = .auto
    @Published var isModelLoaded: Bool = false
    @Published var modelDownloadProgress: Double = 0
    
    // MARK: - Private Properties
    private var whisperKit: WhisperKit?
    
    // MARK: - Public Methods
    func loadModel() async throws
    func transcribe(audioURL: URL) async throws -> TranscriptionResult
    func cancelTranscription()
    
    // MARK: - Model Management
    func isModelDownloaded() -> Bool
    func downloadModel() async throws
    func deleteModel() throws
    var modelSize: Int64 { get }
}
```

**モデル設定:**
- 使用モデル: `small` (約500MB)
- 言語オプション: 自動検出 / 日本語 / 英語

---

### 3. LectureStore

**役割**: 講義データのCRUDと永続化

```swift
@MainActor
class LectureStore: ObservableObject {
    // MARK: - Published Properties
    @Published var lectures: [Lecture] = []
    
    // MARK: - Public Methods
    func addLecture(_ lecture: Lecture)
    func updateLecture(_ lecture: Lecture)
    func deleteLecture(_ lecture: Lecture)
    func getLecture(by id: UUID) -> Lecture?
    
    // MARK: - Persistence
    func loadLectures()
    func saveLectures()
    
    // MARK: - Web Sync
    func markAsSavedToWeb(_ lecture: Lecture, webId: UUID)
    func getPendingUploads() -> [Lecture]
}
```

---

### 4. AuthService

**役割**: Supabase認証管理

```swift
class AuthService: ObservableObject {
    static let shared = AuthService()
    
    // MARK: - Published Properties
    @Published var isAuthenticated: Bool = false
    @Published var currentUser: User?
    @Published var isLoading: Bool = false
    
    // MARK: - Supabase Client
    private let supabase: SupabaseClient
    
    // MARK: - Public Methods
    func signInWithGoogle() async throws
    func signInWithApple() async throws
    func signOut() async throws
    func refreshSession() async
    
    // MARK: - Session Management
    var accessToken: String? { get }
    var isSessionValid: Bool { get }
}
```

---

### 5. SyncService

**役割**: Web同期管理（テキストのみ送信）

```swift
class SyncService: ObservableObject {
    static let shared = SyncService()
    
    // MARK: - Published Properties
    @Published var isSyncing: Bool = false
    @Published var pendingCount: Int = 0
    
    // MARK: - Public Methods
    func saveToWeb(lecture: Lecture) async throws -> UUID
    func retryPendingUploads() async
    
    // MARK: - Queue Management
    private func addToQueue(_ lecture: Lecture)
    private func removeFromQueue(_ lecture: Lecture)
    private func processQueue() async
}
```

**送信データ:**
```swift
struct SaveTranscriptRequest: Encodable {
    let title: String
    let content: String
    let created_at: Date
    let duration: TimeInterval?
    let language: String?
    let app_version: String
}
```

---

## データモデル

### Lecture

```swift
struct Lecture: Identifiable, Codable {
    let id: UUID
    var title: String
    let createdAt: Date
    let duration: TimeInterval
    let audioPath: String  // ファイル名のみ（相対パス）
    var transcriptText: String?
    var transcriptStatus: TranscriptionStatus
    var savedToWeb: Bool
    var webTranscriptId: UUID?
    var updatedAt: Date
    
    // Computed Properties
    var audioURL: URL? {
        // Documents/audioPath から構築
    }
    
    var formattedDuration: String {
        // "01:30:00" 形式
    }
    
    var formattedDate: String {
        // "2026年1月26日" 形式
    }
}
```

### TranscriptionStatus

```swift
enum TranscriptionStatus: String, Codable {
    case none           // 未処理
    case processing     // 処理中
    case done           // 完了
    case error          // エラー
}
```

### TranscriptionResult

```swift
struct TranscriptionResult: Identifiable, Codable {
    let id: UUID
    let text: String
    let fileName: String
    let createdAt: Date
    let language: String?
    let processingTime: TimeInterval?
}
```

### TranscriptionLanguage

```swift
enum TranscriptionLanguage: String, CaseIterable, Codable {
    case auto = "auto"
    case japanese = "ja"
    case english = "en"
    
    var displayName: String {
        switch self {
        case .auto: return "自動検出"
        case .japanese: return "日本語"
        case .english: return "English"
        }
    }
    
    var whisperCode: String? {
        switch self {
        case .auto: return nil
        case .japanese: return "ja"
        case .english: return "en"
        }
    }
}
```

### User

```swift
struct User: Codable {
    let id: UUID
    let email: String?
    let provider: String  // "google" or "apple"
    let createdAt: Date
}
```

---

## 画面設計

### Tab構成

```
┌─────────────────────────────────────────────────────────────┐
│                      TabView                                │
├─────────────────┬─────────────────┬─────────────────────────┤
│    🎙️ Record    │   📚 Library    │      ⚙️ Settings        │
│   (録音画面)     │   (講義一覧)    │      (設定)             │
└─────────────────┴─────────────────┴─────────────────────────┘
```

### 1. RecordView（録音画面）

```
┌─────────────────────────────────────────────────────────────┐
│                        lecsy                                │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│                                                             │
│                      ○ ○ ○ ○ ○ ○ ○                          │
│                   (音声レベルメーター)                       │
│                                                             │
│                     00:00:00.00                             │
│                    (ストップウォッチ)                        │
│                                                             │
│                                                             │
│                    ┌─────────────┐                          │
│                    │             │                          │
│                    │     🔴      │   ← 録音開始/停止         │
│                    │             │                          │
│                    └─────────────┘                          │
│                                                             │
│                 [ 📁 ファイルから選択 ]                      │
│                                                             │
│                                                             │
├─────────────────────────────────────────────────────────────┤
│   🎙️ Record    │   📚 Library    │    ⚙️ Settings           │
└─────────────────────────────────────────────────────────────┘
```

**録音中の状態:**
```
┌─────────────────────────────────────────────────────────────┐
│                        lecsy                                │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│                  🔴 Recording...                            │
│                                                             │
│                    01:23:45.67                              │
│                   (リアルタイム更新)                         │
│                                                             │
│                    ┌─────────────┐                          │
│                    │             │                          │
│                    │     ⏹️      │   ← 停止ボタン            │
│                    │             │                          │
│                    └─────────────┘                          │
│                                                             │
│         ロック画面でも録音は継続します                        │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

### 2. LibraryView（講義一覧）

```
┌─────────────────────────────────────────────────────────────┐
│                       Library                        [ + ]  │
├─────────────────────────────────────────────────────────────┤
│  🔍 検索...                                                 │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 📝 経済学入門 第1回                                   │   │
│  │ 01:32:45  •  2026年1月26日                           │   │
│  │ ✅ 文字起こし完了  •  ☁️ Saved to Web                │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 📝 プログラミング基礎 第5回                           │   │
│  │ 01:15:20  •  2026年1月25日                           │   │
│  │ ✅ 文字起こし完了                                     │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 📝 Recording_20260124_143000                         │   │
│  │ 00:45:30  •  2026年1月24日                           │   │
│  │ ⏳ 文字起こし中... 45%                                │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

### 3. LectureDetailView（講義詳細）

```
┌─────────────────────────────────────────────────────────────┐
│  ← Back                             [ ✏️ ] [ 📤 ] [ 🗑️ ]    │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  経済学入門 第1回                                           │
│  ─────────────────────────────────────────                  │
│  📅 2026年1月26日  •  ⏱️ 01:32:45                           │
│                                                             │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  本日の講義では、マクロ経済学の基本概念について             │
│  説明しました。GDPの定義から始まり、経済成長率、             │
│  インフレーション、失業率などの主要な経済指標に             │
│  ついて解説しました。                                       │
│                                                             │
│  まず、GDPとは国内総生産のことで、一国の経済活動を          │
│  測定する最も重要な指標です。GDPには名目GDPと               │
│  実質GDPがあり、それぞれ異なる用途で使用されます。          │
│                                                             │
│  ...                                                        │
│                                                             │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │             ☁️ Save to Web                          │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │             🌐 Open on Web                          │   │  ← savedToWeb時のみ
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

### 4. SettingsView（設定）

```
┌─────────────────────────────────────────────────────────────┐
│                       Settings                              │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  アカウント                                                  │
│  ─────────────────────────────────────────                  │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 👤 takumi@example.com                               │   │
│  │ Google でログイン中                                  │   │
│  │                                    [ ログアウト ]   │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  文字起こし                                                  │
│  ─────────────────────────────────────────                  │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 言語                                 自動検出  >    │   │
│  └─────────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ モデル                                              │   │
│  │ small (500MB)                        ✅ ダウンロード済│   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  アプリ                                                     │
│  ─────────────────────────────────────────                  │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 言語                                   日本語  >    │   │
│  └─────────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ ストレージ使用量                           1.2 GB   │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  情報                                                       │
│  ─────────────────────────────────────────                  │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ バージョン                                   1.0.0  │   │
│  └─────────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ プライバシーポリシー                            >   │   │
│  └─────────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 利用規約                                        >   │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 設定と権限

### Info.plist

```xml
<!-- マイク権限 -->
<key>NSMicrophoneUsageDescription</key>
<string>講義の音声を録音して文字起こしするためにマイクへのアクセスが必要です。</string>

<!-- バックグラウンドモード -->
<key>UIBackgroundModes</key>
<array>
    <string>audio</string>
</array>

<!-- Live Activities -->
<key>NSSupportsLiveActivities</key>
<true/>

<!-- URL Scheme -->
<key>CFBundleURLTypes</key>
<array>
    <dict>
        <key>CFBundleURLSchemes</key>
        <array>
            <string>lecsy</string>
        </array>
        <key>CFBundleURLName</key>
        <string>com.takumiNittono.lecsy</string>
    </dict>
</array>
```

---

## Live Activities

### LecsyWidgetAttributes

```swift
struct LecsyWidgetAttributes: ActivityAttributes {
    public struct ContentState: Codable, Hashable {
        var recordingDuration: TimeInterval
        var isRecording: Bool
    }
    
    var lectureTitle: String
}
```

### ロック画面UI

```
┌─────────────────────────────────────────────────────────────┐
│  🔴 Recording                                               │
│                                                             │
│                      01:23:45                               │
│                                                             │
│  経済学入門 第1回                                           │
│                                                             │
│  タップしてアプリを開く                                      │
└─────────────────────────────────────────────────────────────┘
```

### Dynamic Island

- **Expanded**: 録音状態、時間、講義タイトル
- **Compact**: 🔴 アイコンと時間
- **Minimal**: 🔴 アイコンのみ

---

## バックグラウンド処理

### 設定

1. **AVAudioSession**: `.playAndRecord` カテゴリ
2. **UIBackgroundModes**: `audio`
3. **Background Task**: `UIApplication.beginBackgroundTask()`

### タイマー管理

```swift
// 開始時刻ベースで累積誤差を防止
recordingStartTime = Date()

timer = Timer(timeInterval: 0.1, repeats: true) { [weak self] _ in
    guard let self, let startTime = recordingStartTime else { return }
    recordingDuration = Date().timeIntervalSince(startTime)
    updateLiveActivity()
}
RunLoop.current.add(timer, forMode: .common)
```

---

## Supabase連携

### 初期化

```swift
let supabase = SupabaseClient(
    supabaseURL: URL(string: "https://xxx.supabase.co")!,
    supabaseKey: "anon-key"
)
```

### 認証

```swift
// Google Sign In
try await supabase.auth.signInWithOAuth(provider: .google)

// Apple Sign In
try await supabase.auth.signInWithApple()
```

### Save to Web

```swift
func saveToWeb(lecture: Lecture) async throws -> UUID {
    guard let token = AuthService.shared.accessToken else {
        throw SyncError.notAuthenticated
    }
    
    let request = SaveTranscriptRequest(
        title: lecture.title,
        content: lecture.transcriptText ?? "",
        created_at: lecture.createdAt,
        duration: lecture.duration,
        language: TranscriptionService.shared.transcriptionLanguage.rawValue,
        app_version: Bundle.main.appVersion
    )
    
    let response = try await supabase.functions.invoke(
        "save-transcript",
        options: .init(body: request)
    )
    
    return response.id
}
```

---

## 依存関係

### Swift Package Manager

```swift
dependencies: [
    .package(url: "https://github.com/argmaxinc/WhisperKit.git", from: "0.9.0"),
    .package(url: "https://github.com/supabase/supabase-swift.git", from: "2.0.0")
]
```

### フレームワーク

| フレームワーク | 用途 |
|--------------|------|
| AVFoundation | 録音・再生 |
| ActivityKit | Live Activities |
| WidgetKit | ウィジェット |
| MediaPlayer | ロック画面制御 |
| SwiftUI | UI構築 |
| Combine | リアクティブ |

---

**最終更新**: 2026年1月26日
